<%= simple_form_for(@son) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :old %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
